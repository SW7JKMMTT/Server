#########################
# General Configuration #
#########################

#################################
# DB Connection properties  #
#################################

# H2 In memory database. Practical for tests and packaging
rest.db.username=sa
rest.db.password=
rest.db.driver=org.h2.Driver
rest.db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
#TODO: You might be able to add ;DATABASE_TO_UPPER=FALSE to the jdbc url
#but it seems like hibernate looks for information_schema in lowercase, while H2 keeps it in UPPER CASE
#so be aware that H2 doesn't care about case sensitivity, but MySQL (production) very much does
